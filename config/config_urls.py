"""
URLs adicionales para archivos movidos a config
"""
from django.urls import path
from . import views

app_name = 'config'

urlpatterns = [
    # Aquí puedes agregar URLs adicionales si es necesario
    # Por ejemplo, para vistas de configuración del sistema
] 